server:
  port: 8002
spring:
  application:
    name: eureka-server
eureka:
  instance:
    hostname: peer2
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://peer1:8001/eureka/,http://peer3:8003/eureka/